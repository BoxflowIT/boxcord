# Boxcord Desktop build assets

Place app icons here:

- `icon.png` — 512x512 PNG (Linux + fallback)
- `icon.ico` — 256x256 ICO (Windows)
- `icon.icns` — macOS icon set
- `tray-icon.png` — 16x16 or 22x22 PNG for system tray
