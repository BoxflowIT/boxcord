# Let Nixpacks auto-detect Node version from .node-version file
[phases.setup]
nixPkgs = ["yarn-1_x", "openssl"]

[phases.install]
cmds = ["yarn install --frozen-lockfile --ignore-engines"]

[phases.build]
cmds = ["yarn run build"]

[start]
cmd = "yarn start"
