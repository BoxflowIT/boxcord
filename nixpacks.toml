# Node.js 20 LTS for Prisma compatibility (supports 20.19+)
[phases.setup]
nixPkgs = ["nodejs_20", "yarn-1_x", "openssl"]

[phases.install]
cmds = ["yarn install --frozen-lockfile --ignore-engines"]

[phases.build]
cmds = ["yarn run build"]

[start]
cmd = "yarn start"
